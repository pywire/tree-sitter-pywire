!path "/test"
!layout "main"

user = "Reece"

---html---
<div class="container" @click={click_handler}>
    <h1>Hello, {user}!</h1>
    {$if count > 0}
        <p>Count is {count}</p>
    {/if}
</div>
